# resources-plugin
wordpress-plugin

This is custom WordPress plugin that creates Custom Post Type (CPT) named `Resources` and provides a shortcode to display the latest resources.

# Features
- Adds a custom post type named Resources
- Includes fields for:
  - Title
  - Featured Image
  - Short Description / Excerpt
- Provides a shortcode

